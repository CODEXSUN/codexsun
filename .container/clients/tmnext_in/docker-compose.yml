name: tmnext-in

services:
  app:
    image: codexsun-app:v1
    pull_policy: never
    build:
      context: ../../..
      dockerfile: .container/Dockerfile
      args:
        VITE_FRONTEND_TARGET: shop
    container_name: tmnext-in-app
    ports:
      - "4007:4000"
      - "5007:4000"
    restart: unless-stopped
    volumes:
      - tmnext_in_runtime:/opt/codexsun/runtime
    networks:
      - codexion-network

volumes:
  tmnext_in_runtime:

networks:
  codexion-network:
    external: true
    name: codexion-network
