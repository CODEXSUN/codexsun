# Zetro 0.1.28 release candidate

## Scope and ownership

The user requested version 0.1.28, a release log, and a production build.
The canonical version helper aligned all workspace, lockfile, Tauri, and Rust versions.
Codex Connection API 0.10.0 owns verified execution and host-written probe files.
Settings web 0.5.0 owns explicit setup and network-policy confirmation.
No database schema or seed changed. Existing accounts and conversations remain unchanged.
The [sandbox record](2026-09-10-windows-sandbox.md) contains the repair and live chat evidence.

## Included behavior

- Official elevated Windows sandbox setup and short-lived enforcement evidence.
- Both direct commands and real agent turns must pass disposable file and network checks.
- Localhost access requires explicit confirmation. Public-network checks remain required under that policy.
- Host-written probe files prevent model transcription errors. Changed probe code invalidates evidence.
- The Settings screen shows policy, state, checks, and expiry-related guidance.
- Lazy loading keeps production JavaScript within the 400 KB budget.
- Turn guidance resolves the Git root and requires PowerShell and native-command error checks.

## Verification and artifact

- Full `npm.cmd run check` passed for 0.1.28. Log: `dist/zetro-0.1.28-check.log`.
- `npm.cmd run desktop:zetro:msi` passed. Log: `dist/zetro-0.1.28-msi.log`.
- Main production JavaScript chunk: 395 KB. No build warning was reported.
- MSI: `dist/apps/zetro/desktop/target/release/bundle/msi/Zetro_0.1.28_x64_en-US.msi`.
- MSI SHA-256: `51EBC7CFBA780F550879E345B7CD1ADD5290F91A099F6A422643304CD0BC49BB`.
- Authenticode status: `NotSigned`. Neither Windows certificate store exposed a code-signing certificate.
- GitHub helper dry run passed. No commit or push occurred.

## Approved installation

The user approved closing Zetro, upgrading, and relaunching with an in-memory supervisor token.
An online backup was saved at `storage/app/private/release-backups/0.1.28/before-install.sqlite`.
The original installed process closed through its main window. Windows Installer returned exit code 0.
Installation log: `dist/zetro-0.1.28-install.log`.
The installed executable reports file and product version 0.1.28.
The installed and built API bundles have identical SHA-256:
`a211f9cb3963ea376865abd8d8c305d73636a255d4995c344d839dd2c077cfe7`.
The relaunched desktop API is ready at `http://127.0.0.1:16050`.
Supervisor authentication succeeded. Database integrity is `ok`, and all seven existing conversations remain present.
The launcher uses the existing UNC database path to avoid the known Codex AppData redirection issue.
No database path is persisted by this launch change.

The user reported a verified UI state, but the post-upgrade API rejected chat task
`81522f82-351c-4948-a519-768af8d666d6` because sandbox evidence was unavailable.
Conversation: `270887f8-388c-49c1-802d-c203027f3cc9`. No project command ran.
The desktop sandbox directory contained the provider directory but no verification probe folder.
The user was asked to distinguish account connection from execution security and repeat verification.
Installed UI sandbox confirmation and a subsequent successful chat remain pending.
Earlier evidence proves two candidate API chats, not a post-upgrade chat in this desktop session.

## Production gates

This is a supervised-development release candidate, not an unattended production certification.
A clean reviewed commit, matching installed UI/restart acceptance, and installer signing remain release gates.
Do not infer business-feature delivery, Docker deployment, or complete network isolation from the chat smoke.
Prompt guidance is not a substitute for enforced command policy or review of tool output.
No commit, push, or deployment occurred. The approved local installation is recorded above.

## Parallel work

Existing sandbox changes from the preceding work are included and preserved.
The version helper updates workspace metadata globally. No unrelated business implementation is changed.
