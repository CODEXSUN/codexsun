# Desktop 0.1.24 candidate

## Scope

The canonical version helper aligned npm workspaces, lockfile, Tauri, Rust, and environment version metadata to 0.1.24.
This candidate includes Automation web 0.4.1, Supervisor API 0.2.1, and the shared authentication block repairs.
Identity web 1.1.2 remains a Platform consumer, not a standalone service bundled in Zetro.
No new database migration is declared.
Existing changes from the preceding Identity and Automation work were preserved.
Source basis: commit `70c5e7e` plus the pending local repairs and canonical version updates, not a clean release commit.

## Verification

`desktop:zetro:msi` passed with no build warning. The largest Zetro web chunk was 392.97 KB.
The executable reports product and file version 0.1.24.
Installer: `dist/apps/zetro/desktop/target/release/bundle/msi/Zetro_0.1.24_x64_en-US.msi`.
SHA-256: `A0B59299ED0BF2AE317E5EE8EF20C1997E813ADE6F273E3AF037245A0894E9B7`.
The installer is unsigned. It has not been installed or published.
Desktop tests passed: two Rust tests and packaged API authentication, repository trust, named-script execution, shutdown, and port release.
Evidence: `dist/desktop-msi-0.1.24.log` and `dist/desktop-test-0.1.24.log`.
The full `npm.cmd run check` source gate passed with exit code 0.
Evidence: `dist/check-release-0.1.24.log`. Negative readiness tests produced expected 503 warning logs; these are not build warnings.
The first source-gate attempt found version-helper formatting in `tauri.conf.json`.
Prettier corrected that file before the gate was restarted. No runtime behavior changed.
This is a candidate build, not a stable approval or public release.
No commit, push, tag, or remote publication is part of this request.

## Next work

Real named-script execution passed through the approved repository trust setting.
P001 disposable three-portal browser acceptance and device activation are prepared for implementation.
Do not infer application adoption, Docker readiness, or business readiness from desktop packaging.

## Automation continuation

The user approved repository trust for the development instance.
Project `00000000-0000-4000-8000-000000000001` was verified against this repository.
Its trust setting is project-specific. Existing push flags, protected branches, and force-with-lease settings were preserved.
Zetro ran `check:ui-system` as durable task `e5ec7f26-85be-48f9-abed-04acf715b7c9`, which completed with exit code 0.
The browser confirmed the unique run label, actual start/completion events, separate summary, and script output.
This verifies a script run, not live agent tool transitions in the installed desktop.

Installed Zetro prepared P001 fixture task `5db6e77f-b7f8-468f-8063-55f282f9c6a0` and completed its read-only review.
The supervisor rejected its proposed port 6110 because the catalog already assigns it to Agent Crew web.
The fixture must use one isolated API and one web origin, with separate browser contexts for the three portals.
Reserve free 6000-series ports through preflight at execution time. Do not replace existing listeners.
Keep bootstrap code in Identity-owned test support, not a production account-creation backdoor.
Provision and remove only a uniquely named disposable database and its grant. Never alter existing user accounts.
The fixture implementation and browser acceptance remain next work, not completed evidence.

References: [Automation evidence](2026-09-10-automation-evidence-clarity.md),
[Identity browser evidence](../platform/2026-09-10-identity-browser-acceptance.md),
and [P001](../../tasks/platform-first-release.md).
