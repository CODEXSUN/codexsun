# Desktop 0.1.20 verification

## Outcome and ownership

The user installed `0.1.19` and requested another version, rebuild, restart, and framework task continuation.
The canonical version helper aligned npm workspaces, internal dependencies, environment metadata, Tauri, and Rust to `0.1.20`.
No migration or seed changed. Existing uncommitted candidate work was preserved.

Zetro Desktop owns packaging and process lifecycle. Developer Tools owns repository task execution.
Framework owns kernel repairs. No business behavior moved into shared packages.

## References and bindings

- [Desktop](../../../apps/zetro/desktop/README.md) owns launch and shutdown behavior.
- [Supervisor](../../../apps/zetro/api/src/modules/supervisor/README.md) owns approved local agent jobs.
- [F001](../../tasks/framework-first-release.md) defines kernel acceptance.
- [Release workflow](../../operations/stable-release-workflow.md) defines downstream gates and approvals.
- [Previous record](2026-09-10-stable-release-workflow.md) preserves the failed review and repaired defects.

## Verified installed baseline

The installed executable reported `0.1.19` at `C:/Program Files/Zetro/zetro-desktop.exe`.
The CLI paired it with an in-memory token on port `16050`.
An idle native close request completed successfully and released that port.
The earlier close timeout did not reproduce in this check. Post-agent shutdown still requires verification.

## Test fixture cleanup

Developer Tools test repositories now set local `core.autocrlf=false` before writing LF fixtures.
This makes Git test input deterministic on Windows. User and project Git settings remain unchanged.
The fix does not suppress Git errors or modify production Git behavior.

## Release status

Zetro review `530a3ed6-eb91-4801-b039-00d4f1a1b48d` read the current candidate source.
It found lost cleanup ownership after failed deactivation or activation rollback.
The job failed because Git rejected the external checkout under the sandbox account.
The saved review remained useful. No global Git trust exception was added.

Two regressions reproduced the cleanup defect. Both passed after the kernel retained failed cleanup modules.
Pending cleanup now blocks install, upgrade, activation, and uninstall until deactivation succeeds.
All nineteen kernel tests pass. The [framework README](../../../packages/framework/README.md) owns these semantics.

Follow-up review `2a44ba27-889d-48b8-be77-57d7071bf748` completed with no failed tool actions.
Conversation `c6c890bd-9b58-4eee-89c3-5ff9230fa8ea` contains the successful two-path verification.
The isolated review worktree stayed clean. The installed desktop exited cleanly after both review jobs.
The earlier native close timeout did not reproduce before or after provider use.

Reviewed kernel source/test SHA-256: `43fce367bb7da3c93ac61e2c7725b4702d43129c738a47d4aff0a8d6808ebba6`.
Hash inputs are sorted `packages/framework/src/*.ts` and `tools/framework-contract.test.mjs`.
Each input contributes its relative path, NUL, raw file bytes, and NUL to SHA-256.
The baseline remains `1a18b7e96d1bd24088b82f28f0fef8e7eff2aaea` plus the uncommitted candidate patch.

The final `check:release:adoption` passed after the cleanup repair, including all 27 workspace builds.
The production JavaScript budget passed for 663 chunks. Kernel tests passed 19 of 19.
Packaged API E2E, MariaDB integration, and runtime catalog validation passed.
The Platform-only profile passed startup, readiness, IPC shutdown, and port release on `6010` and `6021`.
Its live MariaDB preflight passed. Startup reported zero queued migrations and seeds.
Git fixture tests passed without line-ending warnings. Negative health tests still emit their asserted failure logs.

F001 technical implementation and verification are complete for this candidate patch.
`npm.cmd run desktop:zetro:msi` completed successfully for `0.1.20`.
Artifact: `dist/apps/zetro/desktop/target/release/bundle/msi/Zetro_0.1.20_x64_en-US.msi`.
MSI SHA-256: `1AE2AF9A7E08E73817C9F7F259EF06DFCDA0318D1E743A375DD5F93D89845E92`.
The executable reports `0.1.20` and is unsigned.
The final root-dist executable started successfully on port `16050` with an in-memory supervisor token.
It remains running for the user. The MSI upgrade, signing, and production deployment are not verified.
At that build checkpoint, the installed application remained `0.1.19`.
No commit, push, production migration, deployment, or stable approval is implied by this record.

## Installed replacement

The next user request authorized removal and replacement. After a clean desktop
shutdown, the SQLite database was copied to the private backup directory
`storage/app/private/backups/zetro-0.1.19-before-0.1.20`.
Unelevated uninstall failed with MSI error 1730 (administrator permission required).
The approved elevated uninstall and install both exited with code zero.
The installed executable at `C:/Program Files/Zetro/zetro-desktop.exe` now reports
`0.1.20`. It started with in-memory supervisor pairing on port `16050`.
Supervisor capabilities responded successfully. Existing project registration was
retained. No worktree or user-data directory was removed.

P001 read-only review task: `d37b8492-78b2-40ac-ae9c-f954fba1436e`.
The installed desktop is running. Signing and stable publication remain unverified.
