# Modules

Index each module's owner, purpose, public contracts, and dependencies here.

Read the [module registry](registry.md) before a module becomes available to an app or add-on.

Update this index when a module is added, removed, or changes ownership.
