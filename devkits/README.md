# Developer kits

This directory contains developer-oriented applications that use the shared
platform and framework contracts. Each child remains independently owned and
registered in `../core/registry/applications`.

Do not add business data or private cross-application imports here. Install
dependencies from the repository root only; nested `node_modules`, `dist`, and
`.turbo` directories are forbidden.
