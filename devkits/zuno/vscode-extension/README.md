# CodexSun VS Code Extension

The extension supplies the CodexSun sidebar. It obtains no passwords or long-lived tokens from settings. Production activation must use the Zuno browser session and MCP endpoint; logout clears in-memory session state. Destructive actions require a Zuno confirmation response before they are submitted.
