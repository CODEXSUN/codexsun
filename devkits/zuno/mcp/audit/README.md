# Audit

Audit records include request ID, user ID, application ID, tool name, timestamp, success or failure. The audit writer records safe failure text and never stores request secrets or file content.
