# DOCX

This application owns a documentation portal for reading and authoring DOCX
product documentation.

The web host composes the published `DocumentationWorkspace` and
`RichTextEditor` exports. Its portal module owns document navigation, reader
state, Ideas pages, source actions, helper navigation, and in-memory drafts.
