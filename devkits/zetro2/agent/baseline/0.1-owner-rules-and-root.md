# 0.1 Baseline: Root Verification, Change Inspection, and Owner Rules Inventory

- **Date:** 2026-09-24
- **Working Root:** `E:\codexsun\codexsun`
- **Git HEAD:** `88c903e9e28d2bd0bf813a454e946a5311ad508f`
- **Module Owner:** `devkits/zetro2/`

## 1. Root Verification
- Repository root verified: `E:\codexsun\codexsun` matches `AGENTS.md` mandate.
- All commands execute from `E:\codexsun\codexsun`.
- Single repository boundary: no external worktree switching or checkouts.

## 2. Working Tree Changes Inspection
A working-tree check via `git status -s` identified existing changes across:
- `apps/crm/` (API and web modifications)
- `apps/himsx/` (container and config modifications)
- `assist/documentation/CHAGELOG.md` & `assist/operations/deployment-profiles.md`
- `devkits/orship/` (API, web, and container changes)
- `devkits/zcode/` (container scripts, catalog, agent design docs)
- Root configuration (`.gitignore`, `package.json`, `packages/platform-core/`)
- Untracked directories (`apps/temp/dokploy/`, `apps/temp/gitpod/`, `devkits/zetro2/`)

**Policy:** All existing changes outside `devkits/zetro2/` must be preserved intact.

## 3. Applicable Owner Rules Inventory
Sourced from `assist/governance/repository-architecture-rules.md`, `assist/governance/coding-agent-rules.md`, and `AGENTS.md`:

1. **Modular Monolith & DDD:**
   - Every business capability has a single owner module.
   - Zetro2 owns its application code under `devkits/zetro2/`.
   - API modular hierarchy belongs in `devkits/zetro2/api/src/modules/` containing provider, routes, services, repositories, migrations, events, and tests.
2. **Reuse & Dependency Isolation:**
   - Reuse central packages (`packages/*`, `@codexsun/platform-core`) via public contracts only.
   - Strictly no private internal cross-app imports (e.g., from `devkits/zcode` or `apps/*`).
   - Upstream ZVcode source copied to `devkits/zetro2/zvcode` is owned by Zetro2.
3. **File Length Limit:**
   - Max 700 lines for all authored files. Split concerns when approaching limit.
   - Vendor source exception applies to imported upstream editor source in `devkits/zetro2/zvcode/`.
4. **Build and Storage Boundaries:**
   - All build artifacts must output to root `dist/` (e.g. `dist/zetro2/`). Local `dist/` is prohibited.
   - Turborepo cache restricted to `dist/.turbo/<scope>/`.
   - Private storage scoped to `storage/apps/private/zetro2/`.
5. **Contract and Quality Gates:**
   - Zod validation for external contracts and environment variables.
   - Migration integrity: checksummed, append-only migrations.
   - Quality checks: ESLint, TypeScript, Prettier, boundary verification (`check-module-boundaries.mjs`).
