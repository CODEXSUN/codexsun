# 0.2 Baseline: Upstream Source Inventory (`zvcode/` as authoritative copy)

- **Date:** 2026-09-24
- **Working Root:** `/home/workspace/codexsun` (maps `AGENTS.md` `E:\codexsun\codexsun`)
- **Git HEAD:** `940dcd23b57eeff1f79cd582a471a279eec4afc5`
- **Module Owner:** `devkits/zetro2/`

## 1. Source locations

- `apps/temp/openvscode-server`: **absent** in this checkout. `apps/temp/` contains
  `crm, cxapp, dokploy, gitpod, qcafe, windmill` only.
- `devkits/zetro2/zvcode/`: **authoritative copy**, 9,280 git-tracked files.
  Per user direction, inventory proceeds from this in-repo copy.
- `devkits/zetro2/editor/`: integration docs (`README.md`, `UPSTREAM-README.md`,
  `verify-zvcode.mjs`).

## 2. Upstream revision

- `package.json`: name `zvcode`, version `1.110.0`, license `MIT`.
  `package-lock.json` name/version match (`zvcode` / `1.110.0`).
- `product.json`: ZVcode branding (`applicationName`/`serverApplicationName`/
  `urlProtocol`/`embedderIdentifier` = `zvcode`, data folders `.zvcode`).
  No `distro`/Git-HEAD field in current `product.json`; prior import recorded
  distro `bd187e4508a244500eb533c56e5cccb6801a699c` as metadata (see `editor/README.md`).
- `.nvmrc`: `22.22.0`.
- No nested `.git`, `node_modules`, or `out/` inside `zvcode/` (verified absent).

## 3. Local modifications (Zetro2 branding)

- `product.json`: ZVcode names, command, data dir, protocol, platform IDs.
- `package.json` + `package-lock.json`: root identity `zvcode`.
- `README.md`: local product entry, build status (12 lines).
- `build/gulpfile.reh.ts` retains `product.serverApplicationName` /
  `product.applicationName` packaging hooks.

## 4. Licenses and notices (unchanged)

- `LICENSE.txt` (MIT, 1,109 B, sha256 `cce33203a80863c2`): retained.
- `ThirdPartyNotices.txt` (160,174 B, sha256 `19f372f66334cac1`): retained.
- `cglicenses.json` (44K), `cgmanifest.json` (44K): retained.
- `product.json` license URLs still point to
  `https://github.com/gitpod-io/openvscode-server/blob/main/LICENSE.txt`.

## 5. Source size

- `du -sh devkits/zetro2/zvcode`: **171M**.
- Largest trees: `src/` 102M, `extensions/` 42M, `build/` 13M, `resources/` 11M,
  `test/` 1.4M, `cli/` 1.3M.
- Checksums (sha256-16): `product.json` `a74df3eaa5c6f9ff`,
  `package.json` `026968ce70137583`, `README.md` `4996c94d251dd3f3`.

## 6. Verification

- `node devkits/zetro2/editor/verify-zvcode.mjs` → **FAIL (expected)**:
  branding/package/packaging asserts pass; license-compare step throws `ENOENT`
  for `apps/temp/openvscode-server/LICENSE.txt` (source absent here).
  Exit 1, host-linux, 2026-09-24.
- `git ls-files devkits/zetro2/zvcode | wc -l` → 9280.
- Exclusion check (`ls -d zvcode/.git`, `node_modules`, `out/`) → all absent.

## 7. Limitations and next

- No live upstream HEAD to diff; modification list is branding-file scoped.
- No editor compile or browser test in this task (tracked in Phase 2).
- No changelog version bump (inventory only; release review owns versions).
- Next: 0.3 native chat/inline-chat/service inspection.
