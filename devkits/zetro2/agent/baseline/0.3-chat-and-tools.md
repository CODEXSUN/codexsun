# 0.3 Baseline: Native Chat, Inline Chat, Language-Model Services, Extension APIs, Tool Confirmations

- **Date:** 2026-09-24
- **Working Root:** `/home/workspace/codexsun` (maps `AGENTS.md` `E:\codexsun\codexsun`)
- **Git HEAD:** `940dcd23b57eeff1f79cd582a471a279eec4afc5`
- **Module Owner:** `devkits/zetro2/`
- **Scope:** read-only inspection of `devkits/zetro2/zvcode/` surfaces. No vendor-tree edits.

## 1. Native chat (`contrib/chat/`)

- `src/vs/workbench/contrib/chat/{browser,common,electron-browser,test}/`.
- Core contracts: `common/chat.ts`, `common/chatService/chatService.ts` +
  `chatServiceImpl.ts`, `common/model/chatModel.ts`, `chatViewModel.ts`,
  `chatSessionStore.ts`, `common/chatModes.ts` (`IChatModeService`,
  builtin + custom modes, `chat.customModes` storage key).
- Participants: `common/participants/chatAgents.ts`,
  `chatParticipantContribTypes.ts`; registration via `chatParticipants`
  extension point in `browser/chatParticipant.contribution.ts`.
- Request flow: `common/requestParser/chatRequestParser.ts`, variables
  (`attachments/chatVariables.ts`), prompt syntax + hooks
  (`promptSyntax/`, `hookCopilotCliCompat.ts`, `hookClaudeCompat.ts`).
- Model picker: `common/widget/input/modelPickerWidget.ts`, default-model
  contributions (`browser/defaultModelContribution.ts`,
  `planAgentDefaultModel.ts`, `exploreAgentDefaultModel.ts`); initial
  default is `Auto (Vendor Default)`.
- Editing loop: `common/editing/chatEditingService.ts`,
  `chatCodeMapperService.ts`.

## 2. Inline chat (`contrib/inlineChat/`)

- `src/vs/workbench/contrib/inlineChat/{browser,common,electron-browser,test}/`.
- Session: `browser/inlineChatSessionService.ts`
  (`IInlineChatSessionService`, `IInlineChatSession2`) +
  `inlineChatSessionServiceImpl.ts`.
- UI: `inlineChatWidget.ts`, `inlineChatZoneWidget.ts`,
  `inlineChatOverlayWidget.ts`, gutter/editor affordances.
- Commands (`browser/inlineChat.contribution.ts`): start/ask/focus/submit/
  queue/hide, keep (`KeepSessionAction2`), undo, undo-and-close.
- Default model: `browser/inlineChatDefaultModel.ts`.

## 3. Language-model services

- Chat-owned: `contrib/chat/common/languageModels.ts`
  (`ILanguageModelChatMetadata`, `ILanguageModelsService`) and
  `browser/languageModelsConfigurationService.ts`,
  `common/languageModelsConfiguration.ts`, `common/languageModelStats.ts`.
- No `platform/languageModels` service dir; `platform/` has only
  `languagePacks` and `mcp` in this area. Model access flows through the
  chat contribution, not a standalone platform LM service.
- Chat sessions: `contrib/chat/common/chatSessionsService.ts`,
  `browser/chatSessions/`, `model/chatSessionStore.ts`.

## 4. Extension APIs

- `src/vs/workbench/api/common/`: `extHostLanguageModels.ts`
  (`IExtHostLanguageModels`, `ExtHostLanguageModels`),
  `extHostLanguageModelTools.ts` (`ExtHostLanguageModelTools`),
  `extHostChatAgents2.ts`, `extHostChatContext.ts`,
  `extHostChatOutputRenderer.ts`, `extHostChatSessions.ts`,
  `extHostChatStatus.ts`, `extHostMcp.ts`.
- Public contract: `src/vscode-dts/vscode.d.ts` — `chat` namespace
  (`createChatParticipant`), `LanguageModelChat*` messages/parts,
  `LanguageModelChat` (`sendRequest`, tool-call streaming),
  `ChatParticipant`, tool references/tokens.
- Copilot: **not bundled** — `extensions/` (105 entries) has `github`,
  `github-authentication`, `mermaid-chat-features`, but no copilot-chat
  extension. `product.json` `defaultChatAgent` still points at
  `GitHub.copilot` / `GitHub.copilot-chat` with GitHub entitlement URLs.
  Reuse requires the separate Copilot Chat source review (task 0.4).

## 5. Tool confirmations

- Service: `contrib/chat/common/tools/languageModelToolsService.ts`
  (`IToolData`, `ToolDataSource`) +
  `languageModelToolsConfirmationService.ts`
  (`ILanguageModelToolConfirmationActions` with session/workspace/profile
  scopes, pre/post-confirm producers, quick-tree manage items).
- Per-tool confirmations in `common/tools/builtinTools/`:
  `confirmationTool.ts`, `chatExternalPathConfirmation.ts`,
  `chatUrlFetchingConfirmation.ts` (+patterns), `editFileTool.ts`,
  `askQuestionsTool.ts`, `runSubagentTool.ts`, `manageTodoListTool.ts`.
- Tool invocations are first-class chat progress parts
  (`model/chatProgressTypes/chatToolInvocation.ts`).

## 6. Implications for the Zetro2 bridge (planning §7)

- Reuse the native chat + inline-chat surfaces; route model/backend
  selection, file references, approvals, and run history through the
  Zetro2 API bridge (custom layers 3–4).
- Confirmation service scopes (session/workspace/profile) map to Zetro2
  workspace grants and action-bound approvals (task 0.8 design input).
- Do not assume bundled Copilot credentials; record Copilot source,
  API, and entitlement requirements in 0.4 before reuse.

## 7. Verification and limits

- Paths verified with `ls` + `grep` on 2026-09-24, host-linux.
- No code executed (no build/test); no vendor files changed.
- Next: 0.4 Copilot Chat source vs local editor version comparison.
