# 2.3 Import Exclusions: Git Metadata, Dependencies, Caches, Output, Secrets, Runtime Data

- **Date:** 2026-09-24
- **Working Root:** `E:\codexsun\codexsun`
- **Git HEAD:** `119c228f93b112364714fd1a3fd46f1571ce42d5`
- **Scope:** Verify that `devkits/zetro2/zvcode` contains only imported editor source and no excluded Git metadata, dependencies, caches, generated output, secrets, or runtime data; fix any wrongful exclusion of legitimate source.

## Exclusion scan results (host-windows PowerShell)

| Category            | Pattern checked                                                                          | Count |
| ------------------- | ---------------------------------------------------------------------------------------- | ----- |
| Git metadata        | nested `.git` directories                                                                | 0     |
| Dependencies        | `node_modules` directories (recursive)                                                   | 0     |
| Caches              | `.cache`, `.eslintcache`, `.parcel-cache`, `__pycache__`, `*.tsbuildinfo`                | 0     |
| Secrets/environment | `.env*`, `*.pem`, `*.pfx`, `*.key`, `id_rsa`, `id_ed25519`, `credentials`                | 0     |
| Generated output    | root/recursive `out`/`out-*` directories (excluding upstream `build/` scripts), `*.vsix` | 0     |
| Runtime data        | `logs` directories other than the upstream source folder `src/vs/workbench/contrib/logs` | 0     |

Totals: 9,286 files on disk, 139.4 MB. The upstream `zvcode/.gitignore`, `build/` scripts, and `package-lock.json` are retained as source, not excluded artifacts.

## Wrongful exclusion found and fixed

Disk held 9,286 files while Git tracked 9,280. The root `.gitignore` pattern `logs/` (workspace runtime logs) matched the legitimate upstream source folder `devkits/zetro2/zvcode/src/vs/workbench/contrib/logs/`, hiding 6 source files from the index:

- `browser/logs.contribution.ts`
- `common/logs.contribution.ts`
- `common/logsActions.ts`
- `common/logsDataCleaner.ts`
- `electron-browser/logs.contribution.ts`
- `electron-browser/logsActions.ts`

Fix: appended scoped re-include patterns to the root `.gitignore`:

```text
!devkits/zetro2/zvcode/src/**/logs/
!devkits/zetro2/zvcode/src/**/logs/**
```

After the fix, `git ls-files` reports 9,286 paths for `devkits/zetro2/zvcode` (6 newly staged, not committed), and `git ls-files --others --ignored` reports nothing under the tree. Workspace runtime `logs/` and `*.log` ignores remain in force everywhere else.

## Verification commands

```powershell
node devkits/zetro2/editor/verify-zvcode.mjs
git ls-files -- devkits/zetro2/zvcode
git ls-files --others --ignored --exclude-standard -- devkits/zetro2/zvcode
```

Results: `verify-zvcode` passed (branding, package identity, packaging hooks, notices); tracked count 9,286; zero ignored source files remain.

## Limitations

- The 6 recovered files are staged only; no commit was created (commits require explicit user request).
- Exclusion checks are filesystem/Git-index level; no Docker build has run yet (task 2.5/2.8).
