# Orship Web

The web host composes the shared MDI workspace. The Deployments navigation
opens URL-backed views for the overview, providers, targets, applications,
environment records, deployment history, logs, health, and access denied
states. TanStack Query refreshes provider-backed records and sends all actions
through the Orship API. The browser does not access Docker or Dokploy directly.
