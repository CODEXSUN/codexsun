# Application Storage

This folder holds application-scoped runtime files.

The storage provider creates `private/<application>/<module>` and `public/<application>/<module>` namespaces. Do not add live application files to source control.
