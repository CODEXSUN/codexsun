# Task

## Active Batch

- [ ] `#187` Rebuild `apps/crm` into a full CRM platform
  - [ ] Phase 1: architecture and scope reset
    - [x] 1.1 remove the previous completed execution backlog and keep only the active CRM batch
    - [x] 1.2 confirm CRM ownership boundaries across `apps/crm`, `apps/core`, `apps/cxapp`, `apps/api`, and `apps/ui`
    - [x] 1.3 define the first executable delivery slice as `call intake -> task assignment -> reminder -> closure -> audit trail`
  - [ ] Phase 2: CRM domain model and database foundation
    - [x] 2.1 replace the minimal lead and interaction baseline with explicit CRM operational tables for the first slice
    - [ ] 2.2 add stores for accounts, account contacts, leads, opportunities, pipeline stages, calls, call outcomes, assignments, tasks, reminders, notes, activities, campaign records, reputation records, score snapshots, and audit events
    - [ ] 2.3 add reporting-friendly headers, indexes, status history, owner history, and due-date dimensions
  - [ ] Phase 3: shared contracts and backend modules
    - [x] 3.1 add CRM shared schemas and types for the first-slice records, statuses, analytics, and audit views
    - [x] 3.2 split backend code into repositories, services, engines, analytics reads, and audit modules for the first slice
    - [x] 3.3 keep module boundaries small and responsibility-focused
  - [ ] Phase 4: workflow engines and business rules
    - [x] 4.1 build unknown-caller and known-customer intake workflows
    - [x] 4.2 build assignment, reassignment, and ownership-history tracking
    - [x] 4.3 build task, callback, and reminder workflows with snooze, revoke, close, and overdue tracking
    - [x] 4.4 build activity timeline and audit logging
    - [ ] 4.5 build lead scoring, engagement scoring, scoreboards, and leaderboards
    - [ ] 4.6 build campaign and follow-up workflow foundations for updates, launches, invites, and customer response tracking
  - [ ] Phase 5: internal API and permission surface
    - [x] 5.1 add internal CRM routes for intake, boards, tasks, reminders, campaigns, analytics, and audit for the first slice
    - [ ] 5.2 add CRM permission gates for agent, manager, campaign operator, and admin roles
    - [x] 5.3 keep validation at route boundaries and business logic inside CRM services
  - [ ] Phase 6: CRM UI and operator surfaces
    - [x] 6.1 rebuild the CRM workspace navigation and overview for the first slice
    - [ ] 6.2 build the customer 360 board and account or contact-centric views
    - [x] 6.3 build the call desk for unknown callers and known customers
    - [x] 6.4 build task monitor, reminder center, pipeline boards, campaign workspace, analytics, and audit screens for the first slice
    - [x] 6.5 attach technical-name badges to the real CRM shell boundaries
  - [ ] Phase 7: customer engagement and communication workflow
    - [ ] 7.1 support relationship-maintenance workflows including update mails, launch invites, follow-up sequences, and continuous touch history
    - [ ] 7.2 model extension points for digital marketing, reels, posts, and customer replies without claiming fake live integrations
    - [ ] 7.3 keep customer-centric monitoring visible through one CRM board
  - [ ] Phase 8: testing, e2e, and release validation
    - [x] 8.1 add focused service and route tests for the first-slice engines and audits
    - [ ] 8.2 add CRM e2e coverage for intake, assignment, reminder escalation, completion, closure, campaign follow-up, and manager review
    - [ ] 8.3 validate the CRM batch with `npm run typecheck`, `npm run lint`, `npm run test`, and `npm run build`
