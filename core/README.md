# Core

`core/` contains system-owned runtime components.

- `core/platforms/` contains the platform hosts and platform-owned modules.
- `core/registry/` contains application, add-on, and profile metadata.

Business suites belong under `apps/`, developer tools belong under `devkits/`,
and reusable public contracts belong under `packages/`.
