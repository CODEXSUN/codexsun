# Zetro Web

The web host will show ideas, plans, tasks, reviews, worker attempts, test
evidence, deployment rehearsals, and manual approval controls.

The web host consumes public Zetro API contracts and `@codexsun/ui` exports. It
does not run workers or access SQLite directly.
