# Runbooks web

This module composes a master-detail Runbook workspace. The left registry lists
all saved runbooks and supports new-item selection. The right pane shows either
the creation form or one selected runbook, including its schedule, controls,
prompt, initiator, and run history. It calls only the public Zetro Runbooks API.
