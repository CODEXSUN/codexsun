import { useState, type FormEvent, type ReactNode } from "react";
import { PlusIcon, SlidersHorizontalIcon, TriangleAlertIcon } from "lucide-react";
import { Badge } from "@codexsun/ui/components/badge";
import { Button } from "@codexsun/ui/components/button";
import { Empty, EmptyDescription } from "@codexsun/ui/components/empty";
import { Field } from "@codexsun/ui/components/field";
import { Input } from "@codexsun/ui/components/input";
import { NativeSelect, NativeSelectOption } from "@codexsun/ui/components/native-select";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@codexsun/ui/components/tabs";
import type {
  AllergenTagInput, ItemAllergenInput, ItemModifierGroupInput, MenuCatalog, ModifierGroupInput, ModifierOptionInput,
} from "./menu-api";

type Commands = {
  addAllergen: (input: AllergenTagInput) => void;
  addModifierGroup: (input: ModifierGroupInput) => void;
  addModifierOption: (input: ModifierOptionInput) => void;
  assignAllergen: (input: ItemAllergenInput) => void;
  assignModifierGroup: (input: ItemModifierGroupInput) => void;
};

export function MenuCustomizationPanel({ businessId, catalog, commands, pending }: { businessId: string; catalog: MenuCatalog; commands: Commands; pending: boolean }) {
  return (
    <section className="grid gap-5 border-t pt-6">
      <div className="flex items-center gap-2"><SlidersHorizontalIcon className="size-4"/><h3 className="font-semibold">Modifiers and allergens</h3></div>
      <Tabs defaultValue="modifiers">
        <TabsList variant="line"><TabsTrigger value="modifiers">Modifiers</TabsTrigger><TabsTrigger value="allergens">Allergens</TabsTrigger></TabsList>
        <TabsContent className="pt-4" value="modifiers"><ModifierPanel businessId={businessId} catalog={catalog} commands={commands} pending={pending}/></TabsContent>
        <TabsContent className="pt-4" value="allergens"><AllergenPanel businessId={businessId} catalog={catalog} commands={commands} pending={pending}/></TabsContent>
      </Tabs>
    </section>
  );
}

function ModifierPanel({ businessId, catalog, commands, pending }: { businessId: string; catalog: MenuCatalog; commands: Commands; pending: boolean }) {
  const [itemId, setItemId] = useState("");
  const item = catalog.items.find((entry) => entry.id === itemId);
  return <div className="grid gap-6"><div className="grid gap-6 lg:grid-cols-3"><CommandForm disabled={pending} title="New group" onSubmit={(data) => commands.addModifierGroup({ businessId, code: value(data, "code"), maxSelections: Number(data.get("maxSelections")), minSelections: Number(data.get("minSelections")), name: value(data, "name") })}><Field label="Name" htmlFor="name"><Input id="name" name="name" placeholder="Milk choice" required /></Field><Field label="Code" htmlFor="code"><Input id="code" name="code" placeholder="MILK" required /></Field><div className="grid grid-cols-2 gap-3"><Field label="Minimum" htmlFor="custom-minSelections"><Input id="custom-minSelections" name="minSelections" required type="number" defaultValue="0" min="0" /></Field><Field label="Maximum" htmlFor="custom-maxSelections"><Input id="custom-maxSelections" name="maxSelections" required type="number" defaultValue="1" min="1" /></Field></div></CommandForm><CommandForm disabled={pending || !catalog.modifierGroups.length} title="New option" onSubmit={(data) => commands.addModifierOption({ businessId, code: value(data, "code"), groupId: value(data, "groupId"), name: value(data, "name"), priceAdjustmentMinor: Math.round(Number(data.get("priceAdjustment")) * 100), stockItemRef: value(data, "stockItemRef") || undefined })}><Field label="Group" htmlFor="custom-groupId"><NativeSelect className="w-full" id="custom-groupId" name="groupId" required>{catalog.modifierGroups.map((option) => (<NativeSelectOption key={option.id} value={option.id}>{option.name}</NativeSelectOption>))}</NativeSelect></Field><Field label="Name" htmlFor="name"><Input id="name" name="name" placeholder="Oat milk" required /></Field><Field label="Code" htmlFor="code"><Input id="code" name="code" placeholder="OAT" required /></Field><Field label="Price adjustment" htmlFor="custom-priceAdjustment"><Input id="custom-priceAdjustment" name="priceAdjustment" step="0.01" type="number" defaultValue="0" /></Field><Field label="Stock reference" htmlFor="stockItemRef"><Input id="stockItemRef" name="stockItemRef" placeholder="Optional inventory reference" /></Field></CommandForm><CommandForm disabled={pending || !catalog.modifierGroups.length || !catalog.items.length} title="Assign to item" onSubmit={(data) => commands.assignModifierGroup({ businessId, groupId: value(data, "groupId"), itemId: value(data, "itemId"), sortOrder: Number(data.get("sortOrder") || 0), variantId: value(data, "variantId") || undefined })}><Field label="Item" htmlFor="custom-itemId"><NativeSelect className="w-full" id="custom-itemId" name="itemId" required onChange={(event) => setItemId(event.currentTarget.value)}>{catalog.items.map((option) => (<NativeSelectOption key={option.id} value={option.id}>{option.name}</NativeSelectOption>))}</NativeSelect></Field><Field label="Variant" htmlFor="custom-variantId"><NativeSelect className="w-full" id="custom-variantId" name="variantId"><NativeSelectOption value="">All</NativeSelectOption>{(item?.variants ?? []).map((option) => (<NativeSelectOption key={option.id} value={option.id}>{option.name}</NativeSelectOption>))}</NativeSelect></Field><Field label="Group" htmlFor="custom-groupId"><NativeSelect className="w-full" id="custom-groupId" name="groupId" required>{catalog.modifierGroups.map((option) => (<NativeSelectOption key={option.id} value={option.id}>{option.name}</NativeSelectOption>))}</NativeSelect></Field><Field label="Display order" htmlFor="custom-sortOrder"><Input id="custom-sortOrder" name="sortOrder" type="number" defaultValue="0" min="0" /></Field></CommandForm></div><div className="grid gap-3">{catalog.modifierGroups.length ? catalog.modifierGroups.map((group) => <div className="grid gap-2 border-b pb-3" key={group.id}><div className="flex flex-wrap items-center gap-2"><span className="font-medium">{group.name}</span><Badge variant="outline">{group.minSelections}-{group.maxSelections} choices</Badge><Badge variant="outline">{group.options.length} options</Badge></div><div className="text-xs text-muted-foreground">{group.options.map((option) => option.name).join(", ") || "No options yet"} · assigned to {catalog.itemModifierGroups.filter((entry) => entry.groupId === group.id).length} items</div></div>) : <Empty>No modifier groups configured.</Empty>}</div></div>;
}

function AllergenPanel({ businessId, catalog, commands, pending }: { businessId: string; catalog: MenuCatalog; commands: Commands; pending: boolean }) {
  const [itemId, setItemId] = useState("");
  const item = catalog.items.find((entry) => entry.id === itemId);
  return <div className="grid gap-6"><div className="grid gap-6 lg:grid-cols-2"><CommandForm disabled={pending} title="New allergen tag" onSubmit={(data) => commands.addAllergen({ businessId, code: value(data, "code"), name: value(data, "name"), severity: value(data, "severity") as AllergenTagInput["severity"] })}><Field label="Name" htmlFor="name"><Input id="name" name="name" placeholder="Peanuts" required /></Field><Field label="Code" htmlFor="code"><Input id="code" name="code" placeholder="PEANUT" required /></Field><Field label="Severity" htmlFor="custom-severity"><NativeSelect className="w-full" id="custom-severity" name="severity" required>{[{ id: "low", name: "Low" }, { id: "medium", name: "Medium" }, { id: "high", name: "High" }].map((option) => (<NativeSelectOption key={option.id} value={option.id}>{option.name}</NativeSelectOption>))}</NativeSelect></Field></CommandForm><CommandForm disabled={pending || !catalog.allergenTags.length || !catalog.items.length} title="Assign to item" onSubmit={(data) => commands.assignAllergen({ allergenTagId: value(data, "allergenTagId"), businessId, itemId: value(data, "itemId"), note: value(data, "note") || undefined, variantId: value(data, "variantId") || undefined })}><Field label="Item" htmlFor="custom-itemId"><NativeSelect className="w-full" id="custom-itemId" name="itemId" required onChange={(event) => setItemId(event.currentTarget.value)}>{catalog.items.map((option) => (<NativeSelectOption key={option.id} value={option.id}>{option.name}</NativeSelectOption>))}</NativeSelect></Field><Field label="Variant" htmlFor="custom-variantId"><NativeSelect className="w-full" id="custom-variantId" name="variantId"><NativeSelectOption value="">All</NativeSelectOption>{(item?.variants ?? []).map((option) => (<NativeSelectOption key={option.id} value={option.id}>{option.name}</NativeSelectOption>))}</NativeSelect></Field><Field label="Allergen" htmlFor="custom-allergenTagId"><NativeSelect className="w-full" id="custom-allergenTagId" name="allergenTagId" required>{catalog.allergenTags.map((option) => (<NativeSelectOption key={option.id} value={option.id}>{option.name}</NativeSelectOption>))}</NativeSelect></Field><Field label="Note" htmlFor="note"><Input id="note" name="note" placeholder="Contains traces" /></Field></CommandForm></div><div className="grid gap-3">{catalog.allergenTags.length ? catalog.allergenTags.map((tag) => <div className="flex flex-wrap items-center gap-2 border-b pb-3" key={tag.id}><TriangleAlertIcon className="size-4"/><span className="font-medium">{tag.name}</span><Badge variant={tag.severity === "high" ? "destructive" : "outline"}>{tag.severity}</Badge><span className="text-xs text-muted-foreground">Assigned to {catalog.itemAllergens.filter((entry) => entry.allergenTagId === tag.id).length} items</span></div>) : <Empty><EmptyDescription>No allergen tags configured.</EmptyDescription></Empty>}</div></div>;
}

function CommandForm({ children, disabled, onSubmit, title }: { children: ReactNode; disabled: boolean; onSubmit: (data: FormData) => void; title: string }) { return <form className="grid content-start gap-3" onSubmit={(event: FormEvent<HTMLFormElement>) => { event.preventDefault(); const form = event.currentTarget; onSubmit(new FormData(form)); form.reset(); }}><h4 className="font-medium">{title}</h4>{children}<Button className="w-fit" disabled={disabled} type="submit" variant="outline"><PlusIcon/>{title}</Button></form>; }
function value(data: FormData, key: string): string { return String(data.get(key) ?? "").trim(); }
