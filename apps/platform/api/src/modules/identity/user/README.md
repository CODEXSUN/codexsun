# Identity User

This capability owns users and normalized sign-in identifiers. One user can have an email address, username, and mobile number. Credentials remain outside public user responses.

Administrators can manage regular users. Super administrators can view every portal user.
