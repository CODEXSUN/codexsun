# Identity Device

This capability owns device activation for web, desktop, and mobile clients. A device uses a client identifier and a server-issued secret token.

The first verified device becomes active. Later devices stay pending until an active device or super administrator approves them.
