# Identity Security

This capability records login outcomes and safe HTTP activity metadata. It records the actor, subject, device, client type, address, path, outcome, risk, and time.

It never records request bodies, passwords, session tokens, device tokens, or reset secrets. Only super administrators can query the event stream.
