# Identity Verification

This capability defines optional email and mobile OTP provider ports. The disabled provider is active until a deployment selects and configures an approved adapter.

Authentication does not claim that it sent a message while the provider stays disabled.
