# Platform Mobile

This host uses Ionic React and Capacitor. It reads public API contracts only.

The mobile web build writes to `dist/platform/mobile/web/`. Capacitor platform projects and Android or iOS device artifacts require a selected mobile SDK and a separate device verification record.

Do not import Tauri, Rust, desktop commands, repositories, or server modules.
