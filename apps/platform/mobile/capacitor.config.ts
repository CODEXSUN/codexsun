import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.codexsun.platform",
  appName: "CODEXSUN Platform",
  webDir: "../../../dist/platform/mobile/web",
};

export default config;
