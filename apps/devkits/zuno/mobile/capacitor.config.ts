import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = { appId: "com.codexsun.zuno", appName: "Zuno", webDir: "../../../../dist/apps/devkits/zuno/mobile/web" };

export default config;
lt config;
