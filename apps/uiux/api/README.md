# UIUX API Boundary

UIUX does not have an API host. Its catalog data comes from the public UI package registry.

Do not add an API host unless a reviewed UIUX task requires application-owned behavior that cannot remain in `packages/ui`.
