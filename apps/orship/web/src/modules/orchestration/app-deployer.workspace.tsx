import { Button } from '@codexsun/ui/components/button'
import { Download, Terminal } from 'lucide-react'
export function AppDeployerWorkspace() { return <main className="mx-auto flex w-full max-w-5xl flex-col gap-6 p-6"><header><h1 className="text-xl font-semibold">App deployer</h1><p className="text-sm text-muted-foreground">Install the latest verified version from a Repository manager connection.</p></header><section className="rounded-lg border p-5"><label className="text-sm font-medium">Connected repository</label><select className="mt-2 w-full rounded border bg-background p-2"><option>CODEXSUN — main</option></select><div className="mt-5 rounded bg-slate-950 p-4 font-mono text-sm text-slate-100"><Terminal className="mr-2 inline size-4 text-emerald-400"/>bash ./.container/orship/app-deployer.sh --repo codexsun --mode manual</div><p className="mt-3 text-sm text-muted-foreground">Manual mode requires release verification. Automatic installation is held for future Identity-approved representatives.</p><Button className="mt-5"><Download/>Prepare installer</Button></section></main> }
