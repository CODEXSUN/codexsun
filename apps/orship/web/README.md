# Orship Web

The Orship web host composes public `@codexsun/ui` exports. It reads the
browser-safe `VITE_ORSHIP_API_URL` value and shows standalone API composition.

Use `npm.cmd run dev --workspace @codexsun/orship-web` after you create the
ignored `.app.env` file from `.app.env.example`.
