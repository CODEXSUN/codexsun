import type { EngineManifest } from "../../manifests/engine-manifest.js"

export const inventoryEngineManifest: EngineManifest = {
  id: "inventory-engine",
  kind: "engine",
  displayName: "Inventory Engine",
  summary:
    "Reusable inventory foundation for warehouse topology, stock movement, reservation, and availability contracts.",
  dependencies: [],
}
