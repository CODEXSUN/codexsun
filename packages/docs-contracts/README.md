# Docs Contracts

## Purpose

This package owns browser-safe public contracts for the Docs application.

## Public API

Applications import contracts from `@codexsun/docs-contracts`.

## Dependencies

This package uses Zod to validate Docs API data.

## Verification

Run `npm.cmd run check --workspace @codexsun/docs-contracts`.
