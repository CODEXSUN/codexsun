# Platform Core

Platform Core contains reusable adapters and shared packages. It does not contain a deployable application, product domain behavior, or shared web UI.

- `api` contains generic API contracts.
- `web` contains generic web contracts.
- `desktop` contains desktop adapter contracts.
- `shared` contains contracts and mobile UI contracts. Shared web UI and design tokens live in `packages/ui`.
